{
  "buildCommand": "pip install -r requirements.txt --quiet ; python manage.py collectstatic --noinput --clear 2>&1 | grep -E '(copied|post-processed|^[0-9])' || true",
  "framework": "django",
  "env": {
    "DJANGO_SETTINGS_MODULE": "api.settings",
    "PYTHONUNBUFFERED": "1"
  }
}
